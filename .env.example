# Configuración de la API
# Reemplazar con la URL de tu backend
VITE_API_BASE_URL=http://localhost:3000/api

# --- Solo imagen Docker (nginx proxy /api → backend) ---
# Debe ser el nombre del servicio/contenedor en la MISMA red Docker que el front.
# BACKEND_HOST=backend
# BACKEND_PORT=3000
